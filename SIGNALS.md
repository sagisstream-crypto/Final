# SIGNALS.md — калибровка детекторов VolScan на реальной истории

**Дата исследования:** сентябрь 2026. Данные — по 17 сентября 2026 включительно.

> ## ⚠️ Главное, что надо прочитать до всего остального
>
> **Ничего из этого не наблюдалось «вживую».** В окружении, где велась работа,
> исходящие WebSocket-соединения к Binance заблокированы прокси
> (`x-deny-reason: host_not_allowed`), `api.binance.com` и `fapi.binance.com`
> тоже. Живой сканер здесь запустить физически невозможно.
>
> Всё, что ниже, получено **на историческом архиве Binance Vision** —
> официальных помесячных/подневных архивах минутных свечей, и проверено
> **прогоном этих свечей через тот же самый код детекторов**, который работает
> в сервисе (`scanner-service/tools/replay.py` кормит настоящий `Engine`,
> а не копию логики).
>
> **Что нужно сделать вам:** запустить `scanner-service` у себя (на ПК или
> VPS, где Binance доступен) и дать ему поработать несколько недель. Сервис сам
> пишет в SQLite каждый сигнал и цену через 5/15/30/60/240 минут после него —
> то есть вы сможете пересчитать все цифры из этого документа **на своих
> собственных живых данных** и увидеть, сходятся они или нет. Инструкция —
> `scanner-service/README.md`.
>
> Это не инвестиционная рекомендация. Цифры получены на уже известном прошлом;
> на новых данных они будут хуже.

---

## 1. Что изменилось в архитектуре

Старый сканер (`index.html`) — одностраничное браузерное приложение: сам
держал WebSocket к Binance, сам считал, сам рисовал. На телефоне это ломается
по фундаментальной причине — ни iOS, ни Android не дают вкладке держать живой
сокет в фоне, а состояние жило в памяти вкладки.

Теперь это **отдельное приложение**: `scanner-service/` — служба на Python
asyncio, которая работает круглосуточно на ПК или VPS, хранит всё в SQLite и
шлёт алерты в Telegram. Дашборд — **тонкий клиент**, он только читает базу.
Если дашборд закрыт — сканер работает точно так же.

Старый `index.html` **остался в репозитории без изменений** как лёгкий
устаревший вариант. Рекомендуемый — сервис.

| | старый HTML-сканер | новый сервис |
|---|---|---|
| Где работает | вкладка браузера | отдельный процесс, systemd |
| Что при сворачивании | сокет умирает, данные теряются | ничего не происходит, работает |
| Хранение | localStorage, перезапись всего рынка каждый тик | SQLite WAL, пакетная запись раз в 10 с |
| После перезапуска | база пар потеряна | пары, журнал и отслеживание исходов восстанавливаются |
| Телефон | сам считает и сам виснет | просто смотрит на дашборд |
| Фильтры | `localStorage`, у каждого устройства свои | серверные: правятся из браузера и с телефона, применяются к движку сразу, лежат в SQLite |

---

## 2. Данные, на которых всё калибровалось

| | |
|---|---|
| Источник | Binance Vision (официальный архив), через прямой S3-эндпойнт `s3-ap-northeast-1.amazonaws.com/data.binance.vision` |
| Интервал | **1 минута** — это ровно то разрешение, на котором работает сканер |
| Период | **2025-09-01 … 2026-08-31 (12 месяцев)** |
| Корзина | **82 спотовые пары к USDT** |
| Объём данных | 984 помесячных архива, 1.27 ГБ, **≈43.1 млн минутных баров** |
| Отдельно | GENIUSUSDT, KSMUSDT, GUSDT — подневные архивы 06.09–17.09.2026 (спот и перп) |

### Как выбиралась корзина

Из 734 спотовых пар к USDT в архиве отобраны те, что существовали весь период
(347 пар), затем — стратифицированная выборка 82 пар по медианному дневному
обороту: все пары с оборотом > $5 млн/день (40 шт.), плюс случайные 26 из
диапазона $1-5 млн и 16 из $0.3-1 млн.

**Важное наблюдение про фильтр объёма.** Медианный дневной оборот в архиве:
DOGE $82.6M, ZEC $50.1M, TRX $48.1M, PEPE $41.9M, SUI $41.4M, ADA $33.8M…
Пар с оборотом выше $150 млн/день среди альтов **нет вообще**. То есть
поднятие потолка до **$500 000 000** — это не «расширение до крупных альтов»,
это фактически «снять фильтр совсем, кроме BTC/ETH/BNB/SOL/XRP». Именно так
он теперь и настроен по умолчанию.

**Потолок цены ($10 в старой версии) выключен** (`max_initial_price: 0`).
Он молча выбрасывал BNB, LTC, BCH, AAVE, TAO, ZEC, PAXG и другие нормальные
среднекапитальные пары. Цена одной монеты не несёт никакой информации о
вероятности движения — это чисто номинал, зависящий от эмиссии. Если хотите
вернуть — поставьте число в конфиге.

### Дисциплина проверки (без неё цифры — самообман)

1. **Никакого заглядывания в будущее.** Каждый признак в момент *i* считается
   только по барам ≤ *i*. Будущие цены используются исключительно как метка
   исхода.
2. **Дедупликация событий.** Одно большое движение даёт сотни подряд
   «срабатывающих» минут. Считать их сотней независимых попаданий — классический
   способ показать красивую цифру. Все события схлопываются с кулдауном 2 часа
   на пару: одно движение = одно событие.
3. **Двойной walk-forward.** Монеты детерминированно делятся пополам
   (coinA / coinB), время делится 8 месяцев / 4 месяца (`t_in` / `t_out`).
   Пороги подбирались **только на coinA + t_in**, а в таблицах ниже рядом
   показаны coinB и t_out — то, что подбором не трогали.
4. **Базовая частота всегда рядом.** Без неё «15 % попаданий» ничего не значит.

### Базовые частоты (случайная минута случайной пары из корзины)

| Порог | 1 час | 2 часа | 4 часа | 6 часов | 12 часов | 24 часа |
|---|---|---|---|---|---|---|
| +3 % | 2.50 % | 5.64 % | 11.56 % | 16.66 % | — | — |
| +5 % | 0.70 % | 1.73 % | 4.03 % | 6.41 % | — | — |
| **+10 %** | **0.14 %** | **0.34 %** | **0.83 %** | **1.40 %** | **3.3 %** | **7.3 %** |
| +15 % | 0.06 % | 0.14 % | 0.34 % | 0.56 % | — | — |

Основная метрика дальше — **P(+10 % в течение 4 часов), база 0.83 %**.

---

## 3. Вопрос 1 — «сжатие просыпается». Честный ответ: это не работает

Самый неприятный, но самый надёжный результат исследования.

### 3.1 Старый порог сжатия вообще ничего не фильтровал

В v16 сжатие определялось как `COMPRESSION_RANGE_PCT = 1.4 %` — диапазон цены
за 15 минут меньше 1.4 %. Медианный 15-минутный диапазон у пар корзины — **0.56 %**,
а условие `range15 ≤ 1.4 %` выполнялось на **94.3 %** всех «горячих» баров.

Условие, истинное в 94 % случаев, не несёт информации вообще. Это был не
фильтр, а константа.

### 3.2 Чем УЖЕ диапазон, тем ХУЖЕ шансы

Берём одинаковый триггер (`RVOL ≥ 30` и `+0.6 %/1м`, сам по себе 4.6 %
попаданий) и режем его по ширине 15-минутного диапазона в момент срабатывания:

| Диапазон за 15 мин | событий | P(+10 %/4ч) | coinB | t_out |
|---|---|---|---|---|
| 0 – 0.4 % (мёртвая тишина) | 1 774 | **1.4 %** | 1.8 % | 0.7 % |
| 0.4 – 0.8 % | 2 856 | 2.5 % | 2.8 % | 1.7 % |
| 0.8 – 1.4 % | 2 723 | 4.1 % | 4.5 % | 3.4 % |
| 1.4 – 3.0 % | 2 641 | 6.8 % | 7.4 % | 5.3 % |
| > 3.0 % (уже трясёт) | 1 653 | **15.9 %** | 15.9 % | 14.3 % |

Зависимость монотонная, держится на обеих половинах монет и на обоих периодах.
Разница одиннадцатикратная — и **в сторону, противоположную общепринятой**.

### 3.3 «Тихо, объём пошёл, но цена ещё не двинулась» — хуже случайного

Самая «опережающая» версия сигнала, которую вы просили:

| Правило | событий | P(+10 %/4ч) |
|---|---|---|
| узкий диапазон + `vol_z ≥ 4` + `RVOL ≥ 8` + `0 ≤ Δ%1м < 0.6` | 31 058 | **0.4 %** |
| то же, диапазон ещё уже | 11 422 | **0.2 %** |
| *база* | — | *0.83 %* |

То есть покупать «тихую монету, где зашевелился объём, но цена ещё стоит» —
статистически **хуже**, чем покупать случайную минуту случайной монеты.

Детектор `WAKE` («выход из сжатия») в чистом виде даёт **2.0 % попаданий**
(127 удач против 6 119 промахов) — формально выше базы, практически — шум.

### 3.4 Это же подтвердил и параллельный бэктест

Соседнее исследование в этом же репозитории (`STRATEGY.md`, 2.29 млн часовых
баров, 49 монет, 2021-2026) независимо получило то же самое:
сжатие полос Боллинджера (`bb_width_pct < 0.25`) даёт **lift 0.49** — ровно
вдвое хуже случайного. А лучшее их правило — `vol_z > 4` И `RSI(14) < 45` И
**широкий** канал (`bb_width_pct > 0.6`), lift 13.9×.

Два независимых исследования, разные таймфреймы, разные корзины, разные
определения события — один вывод: **движения рождаются там, где уже трясёт, а
не там, где тихо.**

### 3.5 Что с этим сделано в коде

* `COMPRESSION_RANGE_PCT = 1.4 %` заменён на **относительный** порог:
  сжатие = 15-минутный диапазон ≤ 0.7 от того, что эта пара сама печатала
  за последние сутки (`compression_rel_max`). Фиксированный процент бессмыслен:
  1.4 % для PAXG — буря, для мем-коина — штиль.
* В composite score **убран бонус за сжатие** (+6 в v16). Вместо него добавлен
  **бонус за расширение диапазона** (+6).
* Детектор `WAKE` оставлен, но **никогда не шлёт Telegram** — только строка в
  журнале и бейдж «СЖАТИЕ» в дашборде. В сообщении такого типа явно написано,
  что это контекст, а не сигнал на вход.
* Добавлено фоновое условие из соседнего бэктеста: `RSI(14) < 45` +
  широкий канал по часовым свечам (`HourlyContext`, обновляется по REST).
  Даёт +6 к score, а вместе со всплеском `vol_z > 4` (их лучшее правило) — +12.

---

## 4. Вопрос 2 — аномальный объём. Z-оценка сама по себе бесполезна

### 4.1 Голый z-score не предсказывает ничего

| Правило | событий | P(+10 %/4ч) | coinB | t_out |
|---|---|---|---|---|
| `vol_z ≥ 3` | 319 438 | 0.8 % | 0.9 % | 0.8 % |
| `vol_z ≥ 6` | 276 742 | 0.8 % | 0.9 % | 0.8 % |
| `vol_z ≥ 10` | 214 447 | 0.8 % | 0.8 % | 0.7 % |
| `vol_z ≥ 20` | 118 056 | 0.7 % | 0.8 % | 0.6 % |
| `объём / медиана 20 мин ≥ 20` | 218 769 | 0.6 % | 0.7 % | 0.6 % |
| *база* | | *0.83 %* | | |

Плоско на уровне базы, а «кратность к медиане» — даже ниже.

**Почему.** Z-оценка = (текущий объём − медиана) / σ. На мёртвой паре медиана и
σ уходят почти в ноль, и одна сделка на $300 даёт z = 300. Без абсолютного пола
z-оценка — это не «аномалия», а детектор мёртвой ленты. В таблице признаков
(раздел 7) видно то же самое с другой стороны: `vol_z`, `vol_mult`, `ats_x` и
`shelf_cv` все антипредиктивны, и все четверо взрываются на одном и том же —
на пустой базе.

### 4.2 Что работает: RVOL (оборот против собственной средней минуты) + цена

| Правило | событий | P(+10 %/4ч) | coinB | t_out |
|---|---|---|---|---|
| `RVOL ≥ 30` | 47 298 | 1.4 % | 1.5 % | 1.1 % |
| `RVOL ≥ 100` | 6 126 | 3.2 % | 3.3 % | 2.1 % |
| `RVOL ≥ 30` и `Δ%1м ≥ 0.6` | 9 903 | 4.6 % | 5.0 % | 3.4 % |
| `RVOL ≥ 30` и `Δ%1м ≥ 1.0` | 5 136 | 7.2 % | 7.7 % | 5.3 % |
| **`RVOL ≥ 30` и `Δ%1м ≥ 2.0`** | 1 406 | **15.6 %** | 15.7 % | 13.2 % |

Подтверждение ценой даёт в разы больше, чем любая тонкая настройка объёмного
порога. Это неприятно (значит, «предсказать заранее» толком нельзя), но это
факт, и он совпадает с выводом соседнего бэктеста: **всплеск объёма не
опережает движение, он с ним совпадает**.

### 4.3 Что сделано в коде

Детектор `ANOMALY` требует одновременно:

* `vol_z ≥ 6`, где **знаменатель z-оценки снизу ограничен** 25 % собственной
  средней минуты пары (`indicators.zscore(..., floor=...)`) — это и убивает
  пыль;
* `RVOL ≥ 30` — абсолютный пол по размеру;
* `Δ%1м ≥ 0.6` — подтверждение ценой.

По умолчанию `ANOMALY` **не шлёт Telegram** (`alert_anomaly: false`): он почти
всегда является частью эпизода, по которому уже ушёл ⚡/🚀, и отдельное
сообщение было бы ровно тем спамом, на который вы жаловались. В журнале и
дашборде он есть.

---

## 5. Вопрос 3 — накопление. Построено, измерено, преимущества нет

Накопление реализовано так, как вы описывали: устойчивое давление покупателей
при стоящей цене. Считается скользящее окно (20 мин в сервисе, проверялись
также 1 ч и 4 ч):

* `taker_w` — доля агрессивных покупок, взвешенная по обороту;
* `taker_frac` — доля минут окна, где покупатели преобладали;
* `net_flow_x` — чистый покупной поток (OBV-подобный), нормированный на
  собственную среднюю минуту пары;
* цена при этом должна стоять: диапазон ≤ 0.9 от собственной нормы.

### 5.1 Итог на 12 месяцах: ниже базы при любых порогах

Цель +10 % за 24 часа, база **7.3 %**:

| Пороги (taker_w / доля минут / поток / диапазон) | событий | на монету/мес | P(+10 %/24ч) | coinB | t_out |
|---|---|---|---|---|---|
| 0.56 / 0.60 / 0.15 / 1.1 | 71 391 | 72.6 | 6.87 % | 7.51 % | 7.14 % |
| 0.58 / 0.65 / 0.25 / 0.9 | 17 310 | 17.6 | 6.23 % | 7.14 % | 6.13 % |
| 0.60 / 0.70 / 0.25 / 0.9 | 11 084 | 11.3 | 6.13 % | 7.04 % | 6.21 % |
| 0.62 / 0.75 / 0.25 / 0.8 | 8 732 | 8.9 | 5.85 % | 6.82 % | 5.79 % |
| 0.58 / 0.65 / 1.00 / 0.9 | 1 529 | 1.6 | 5.82 % | 6.80 % | 5.59 % |
| *база* | | | *7.3 %* | | |

И по более короткой цели (+5 % за 4 часа, база 4.03 %) — то же самое:
2.4-3.6 %, то есть тоже ниже базы.

**Чем сильнее перекос в покупку, тем хуже результат.** Отдельная проверка
«доли покупок» по всей корзине даёт ту же картину: `taker_w20 ≥ 0.55` → 0.8 %,
`≥ 0.70` → 0.5 % (база 0.83 %). Соседний бэктест на часовых свечах получил
идентичное: `taker_buy > 0.55` — **lift 0.48**.

Единственное, что хоть немного работает, — не *перекос*, а *масштаб* потока:
`net_flow_x ≥ 4` → 3.6 % против базы 0.83 %. Но это почти то же самое, что
«большой объём», то есть RVOL под другим именем.

### 5.2 Ваши два примера: KSM и G (17.09.2026)

Обе пары нашлись в архиве (GUSDT — недавний листинг, но данные есть).
На мягких порогах (taker_w ≥ 0.56, доля минут ≥ 0.60, поток ≥ 0.15,
диапазон ≤ 1.1) за 6-17 сентября:

**KSMUSDT (перп), 12 эпизодов.** Самые интересные:

| Окно (UTC) | длит. | диапазон | taker_w | бычьих минут | поток | +4ч макс | +4ч мин |
|---|---|---|---|---|---|---|---|
| 17.09 11:18–11:44 | 27 мин | 1.27 % | 0.688 | 0.60 | ×0.22 | **+7.32 %** | −0.28 % |
| 17.09 12:38–12:47 | 10 мин | 1.01 % | 0.643 | 0.60 | ×0.18 | **+24.96 %** | −0.55 % |
| 15.09 00:33–00:59 | 27 мин | 1.20 % | 0.747 | 0.63 | ×0.30 | +2.22 % | −0.61 % |
| 12.09 01:47–02:41 | 55 мин | 2.13 % | 0.788 | 0.63 | ×0.41 | +0.90 % | −0.45 % |
| 12.09 16:11–16:40 | 30 мин | 0.42 % | 0.639 | 0.60 | ×0.19 | +0.79 % | −0.24 % |

Итог дня 17.09 по KSM: **+6.3 %**, дневной диапазон 27.3 %, оборот $41.4 млн
на перпе. Накопление действительно было — и два эпизода из двенадцати
действительно предшествовали движению.

**GUSDT.** Спот: 4 эпизода, лучший 15.09 13:57–14:09 (13 мин, диапазон 1.56 %,
taker_w 0.825) → +7.16 % за 4 часа. Перп: 27 эпизодов, почти все ушли в ноль
(+0.3…+3.5 %). Итог дня 17.09 по G: **+12.3 %**, дневной диапазон 29.8 %.

**Как это честно читать.** В ваших двух примерах накопление было видно, и в
паре случаев за ним пошло движение. Но 2 из 12 у KSM и ~1 из 31 у G — это и
есть те самые 5-7 %, что мы получили на всей корзине из 82 монет за 12
месяцев. Глазами такие эпизодыы запоминаются («вот же, я видел!»), а те
двадцать, что не сработали, — нет. Это классическая ошибка выжившего, и
именно от неё защищает подсчёт по всей корзине.

### 5.3 Что сделано в коде

* Детектор `accumulating` построен и работает: бейдж **🐋 НАКОПЛЕНИЕ** в
  дашборде, отдельная вкладка, запись в журнал.
* Он **не добавляет ни одного балла** в composite score.
* Telegram по нему **выключен по умолчанию** (`alert_accumulation: false`) —
  на замеренных порогах это 17 срабатываний на монету в месяц при нулевом
  преимуществе, то есть чистый спам. Включается одной строкой в конфиге, и
  сообщение честно предупреждает, что преимущества нет.

---

## 6. Вопрос 2b (главный) — детектор непрерывного УСКОРЕНИЯ

Это то, что вы описали как основное: пары, где **цена И объём растут
одновременно и непрерывно**, а не дёргаются одним всплеском.

### 6.1 Правило

На окне из **20 слотов по 30 секунд = 10 минут** (в исследовании — 10 баров
по 1 минуте; математика идентична):

1. МНК-прямая по цене: наклон > 0 и **R² ≥ 0.55**;
2. МНК-прямая по обороту (в единицах RVOL): наклон > 0 и **R² ≥ 0.30**;
3. окно делится на **три равные трети**, и средняя цена И средний оборот
   должны расти от первой трети ко второй и от второй к третьей;
4. суммарный рост цены за окно **≥ 2 %**;
5. текущий `RVOL ≥ 12`;
6. **дебаунс:** всё вышеперечисленное должно продержаться **2 слота подряд**
   (≈1 минута), и только тогда зажигается бейдж.

Пункты 1-3 — это и есть защита от «дёрганья»: одиночный всплеск в конце
плоского окна даёт отвратительный R² и оставляет первые две трети одинаковыми.

### 6.2 Доказательство, что 1-2 тика не проходят

**Синтетическая инъекция.** Берём 40 000 реальных плоских участков ленты
(20 монет × 2 000 случайных отрезков), выравниваем их и приклеиваем сверху
искусственную фигуру:

| Что вклеено | сработало |
|---|---|
| одиночный бар: цена +3 %, объём ×60 | **0 из 40 000** |
| два бара подряд: +1.5 % / +3 %, объём ×40 / ×60 | **0 из 40 000** |
| честный 10-барный разгон: цена 0.3 % → 3 %, объём ×3 → ×40 | **40 000 из 40 000** |

Нулевая ложная сработка на блипах и стопроцентная на настоящем разгоне.

**На реальной ленте.** Доля срабатываний, попавших в изолированный всплеск
объёма длиной ≤ 2 минуты:

| Детектор | срабатываний | доля «блипов» | на монету/мес | P(+10 %/4ч) |
|---|---|---|---|---|
| «наивный» `RVOL ≥ 30` и `Δ%1м ≥ 0.6` | 10 193 | **64.8 %** | 10.4 | 4.6 % |
| ускорение, без дебаунса (persist = 1) | 11 392 | 76.9 % | 11.6 | 5.9 % |
| ускорение, persist = 2 | 3 525 | 46.5 % | 3.6 | 10.2 % |
| **ускорение, итоговое (рост ≥ 2 %, RVOL ≥ 12, persist = 2)** | **1 286** | **14.9 %** | **1.31** | **17.5 %** |
| то же, persist = 3 | 576 | **0.0 %** | 0.59 | 19.6 % |

### 6.3 Итоговые цифры детектора ускорения (walk-forward)

Правило: окно 10 мин, R² цена ≥ 0.55, R² объём ≥ 0.30, три растущие трети,
рост ≥ 2 %, RVOL ≥ 12, дебаунс 2 сэмпла.

| Срез | событий | P(+10 % за 4 ч) |
|---|---|---|
| всё | 1 286 | **17.5 %** |
| coinA (на чём подбиралось) | 496 | 17.9 % |
| **coinB (не трогали)** | 790 | **17.2 %** |
| t_in (первые 8 мес) | 937 | 18.5 % |
| **t_out (последние 4 мес, не трогали)** | 349 | **14.9 %** |
| *база* | | *0.83 %* |

Lift ≈ **21× к базе**. Разница coinA/coinB — 0.7 п.п., то есть на незнакомых
монетах правило не разваливается. По времени просадка с 18.5 % до 14.9 % —
обычная плата за то, что мир меняется; её надо ожидать и дальше.

**Ложные срабатывания: 82.5 %.** Медианная просадка после срабатывания
−3.85 %. То есть это **не «покупай»**, это «смотри сюда прямо сейчас».

### 6.4 Реальный случай: GENIUS/USDT, 17.09.2026

Ваши скриншоты алертов старого сканера — это ground truth. Соответствующие
минутные архивы в Binance Vision есть (и спот, и перп), они скачаны и
**прогнаны через настоящий движок нового сервиса**
(`python3 tools/replay.py GENIUSUSDT 2026-09-14 2026-09-17`).
Время на скриншотах — местное, UTC+5.

Лента (спот, UTC):

| UTC | местное | цена | Δ%1м | RVOL | Δ1м $ | тейкер |
|---|---|---|---|---|---|---|
| 11:15–11:32 | 16:15–16:32 | 0.3049 | 0.00 | 0–11 | 0–1 362 | — |
| **11:33** | **16:33** | 0.3085 | +1.31 | 216 | 27 029 | 0.76 |
| 11:34 | 16:34 | 0.3123 | +1.23 | 170 | 24 449 | 0.57 |
| 11:36 | 16:36 | 0.3103 | −1.30 | 43 | 7 251 | 0.24 |
| **11:37** | **16:37** | 0.3211 | +3.48 | 288 | 50 275 | 0.72 |
| 11:38 | 16:38 | 0.3386 | +5.45 | 893 | 186 922 | 0.61 |
| 11:40 | 16:40 | 0.3507 | +7.28 | 494 | 218 426 | 0.68 |
| **11:41** | **16:41** | 0.3803 | **+8.44** | 461 | 273 766 | 0.53 |
| 11:46 | 16:46 | 0.4293 | +5.95 | 262 | 390 085 | 0.57 |

Что выдал новый движок на этой ленте:

| UTC | местное | сигнал | score |
|---|---|---|---|
| **11:33:30** | **16:33:30** | ⚡ РАННИЙ ПРОБОЙ · RVOL ×188 · +1.31 %/1м · ×1.19 от базы | 67 |
| **11:37:30** | **16:37:30** | 🚀 УСКОРЕНИЕ · цена и объём растут 10 мин подряд: +5.3 %, R² цена 0.76 / объём 0.55 | 96 |
| **11:38:00** | **16:38:00** | 🔥 АНОМАЛЬНЫЙ ВЫНОС — УСКОРЯЕТСЯ · RVOL ×188 → ×432, Δ1м $27 029 → $118 585 без отката | 100 |

Сравните с вашим первым алертом: 16:33, RVOL ×129, +1.28 %/1м, ×1.13 от базы.
Совпадение практически поминутное (RVOL считается от чуть другой базы, отсюда
188 против 129).

**Запас по времени.** От ⚡ в 11:33:30 до взрывной свечи в 11:41 — **7.5 минут**.
От 🚀 в 11:37:30 — **3.5 минуты**. От цены на момент 🚀 (0.3211) максимум
следующих 4 часов — **+41.7 %** (0.4550).

**И главное — сколько сообщений.** Старый сканер за эти 13 минут прислал
**девять** штук (EARLY спот, EARLY перп, EARLY по другой паре, SCORE×2 спот,
SCORE×2 перп, RVOL 346+, RVOL 200+, RVOL 236+, SCORE×2 перп). Новый — **три**,
каждое сильнее предыдущего. За все 4 суток прогона по этой паре — **6**
telegram-алертов всего.

---

## 7. Вопрос 4 — «какая пара даст +10 %». Честные цифры

### 7.1 Что вообще предсказывает, а что нет

Ранжирование признаков по доле попаданий в верхних процентилях
(in-sample, coinA + t_in, 162 302 бара с RVOL ≥ 10, сырая доля 2.76 %):

| Признак | p50 | p90 | p99 | вывод |
|---|---|---|---|---|
| диапазон за 15 мин | 5.2 % | 17.3 % | **42.8 %** | сильнейший — и это *расширение* |
| Δ% за 5 мин | 3.4 % | 11.1 % | **36.1 %** | импульс |
| диапазон 4ч / своя норма | 4.6 % | 13.5 % | 36.9 % | «уже трясёт» |
| Δ% за 2 мин | 3.2 % | 10.1 % | 32.0 % | |
| Δ% за 1 мин | 3.0 % | 8.8 % | 29.9 % | |
| расстояние до 24ч хая | 2.8 % | 5.1 % | 17.7 % | |
| дневной оборот пары | 3.2 % | 4.9 % | 12.2 % | крупные ходят чаще |
| RVOL | 3.1 % | 4.5 % | 8.4 % | работает, но слабее цены |
| чистый покупной поток | 3.2 % | 5.0 % | 7.8 % | слабо |
| **`vol_z`** | 1.1 % | 0.7 % | 1.2 % | **антипредиктивен** |
| **средний чек сделки** | 0.9 % | 0.6 % | 0.4 % | **антипредиктивен** |
| **доля покупок тейтеров** | 3.1 % | 0.7 % | 0.4 % | **антипредиктивна** |
| **`объём / медиана`** | 1.1 % | 1.1 % | 1.0 % | **антипредиктивен** |

### 7.2 Итоговое правило «КАНДИДАТ +10 %»

```
RVOL ≥ 20
Δ% за 5 минут ≥ 2.0 %
диапазон 15 мин ≥ 0.55 от нормы пары   (расширение, не сжатие)
до 24-часового хая ≥ −1 %
цена в верхних 10 % своего 4-часового диапазона
доля покупок тейкеров ≥ 0.60 (или неизвестна)
```

| Срез | событий | P(+10 % за 4 ч) |
|---|---|---|
| всё | 908 | **18.9 %** |
| coinA (подбор) | 307 | 21.2 % |
| **coinB (не трогали)** | 601 | **17.8 %** |
| t_in | 599 | 20.9 % |
| **t_out (не трогали)** | 309 | **15.2 %** |
| *база* | | *0.83 %* |

Прочие цели по тому же набору событий:

| Цель | всё | coinB | t_out |
|---|---|---|---|
| +5 % за 4 ч | 34.6 % | 32.6 % | 34.0 % |
| +10 % за 6 ч | 20.5 % | 19.3 % | 17.8 % |
| +10 % за 12 ч | 24.3 % | 22.5 % | 24.3 % |
| +20 % за 12 ч | 10.9 % | 9.8 % | 12.0 % |

### 7.3 Три цифры, которые важнее «18.9 %»

**Ложные срабатывания — 81.1 %.** Четыре из пяти сигналов до +10 % не доходят.

**Медианный исход — отрицательный.** Медианный максимум за 4 часа после
сигнала всего **+3.03 %**, медианная просадка **−4.02 %**, и только в **30 %**
случаев цена через 4 часа выше, чем была. Распределение крайне асимметричное:
живёт всё на редких хвостах. Без стопа и без готовности дать победителю
бежать это не торгуется, а просто теряет деньги.

**Полнота — 4.4 %.** Из 2 877 реальных эпизодов «+10 % за 4 часа» в корзине
сигнал заранее (в пределах 2 часов до) пометил только **128**. Это сигнал с
высокой точностью и очень низкой полнотой: он не найдёт большинство движений,
зато то, что нашёл, — заметно лучше случайного.

**Разнообразие.** 908 событий распределены по 77 парам из 82, попадания есть
у 43 пар, на три самые «удачные» пары приходится 20 % попаданий. То есть
результат не держится на паре счастливых монет.

**Частота.** ≈2.5 срабатывания в сутки на 82 монеты.

### 7.4 Что показал профиль события (это и есть ответ «как выглядит»)

Для событий-победителей и событий-промахов усреднены траектории признаков
в окне ±30 минут вокруг триггера:

| Минута относительно сигнала | −30 | −10 | −5 | −1 | **0** | +5 | +15 | +30 |
|---|---|---|---|---|---|---|---|---|
| цена, % к триггеру (победители) | **−4.82** | −3.95 | −3.37 | −1.39 | 0 | +0.83 | +1.87 | **+3.43** |
| цена, % к триггеру (промахи) | −4.27 | −3.25 | −2.66 | −1.07 | 0 | −0.39 | −0.75 | −1.36 |
| RVOL (победители) | 0.4 | 0.6 | 1.0 | 6.9 | 26.6 | 6.7 | 3.4 | 2.0 |
| 15-мин диапазон (победители) | 1.33 | 1.85 | 2.36 | 3.65 | 4.17 | 7.58 | 8.26 | 6.34 |
| доля покупок (победители) | 0.51 | 0.53 | 0.57 | 0.63 | 0.69 | 0.54 | 0.52 | 0.50 |

Ключевое: **за 30 минут до сигнала цена была на ~4.8 % НИЖЕ уровня триггера.**
То есть движение рождается не на пробое хая после тишины, а **на выкупе после
пролива**. Диапазон при этом весь час только расширялся. Это ровно то, что
независимо нашёл соседний бэктест на часовых свечах (RSI 44.6, цена на 6.9 %
ниже суточного хая, преобладание продаж в момент старта).

Разделить победителей и промахов *до* триггера практически невозможно —
траектории почти совпадают. Расходятся они только после: у победителей
диапазон продолжает расширяться (8.26 против 3.81 на +15 мин). Это и есть
математическое обоснование того, зачем нужен детектор **ускорения** —
подтверждение приходит не до события, а в первые минуты после его начала.

### 7.5 Чего не хватает, чтобы стало лучше

Честно: 15-19 % — это потолок того, что достаётся из публичных минутных
свечей. Чтобы двигаться дальше, нужны данные, которых в klines нет:

1. **Стакан (глубина, а не только лучшая цена).** Сейчас перекос виден только
   по верхушке стакана из тикера, а на перпах его нет вообще. Реальное
   поглощение предложения видно в глубине — это `@depth20` стрим.
2. **Лента сделок (`aggTrade`).** Даст размеры отдельных сделок и кластеры —
   средний чек из klines слишком грубый (и, как показано, антипредиктивен).
3. **Фандинг и открытый интерес на перпах.** Рост OI при стоящей цене — куда
   более прямой индикатор набора позиции, чем taker-ratio на споте.
4. **Кросс-секция.** В ваших скриншотах GENIUS и MMT загорелись в одну минуту.
   Одновременный разгон нескольких пар — сильный признак, который в текущей
   архитектуре не используется вообще. Это, вероятно, самый дешёвый
   следующий шаг.
5. **Внешние триггеры** (листинги, новости) — источник большинства резких
   выносов, и в свечах их нет по определению.

---

## 8. Сводка изменений в детекции

| Что | Было (v16, HTML) | Стало (сервис v17) | Почему |
|---|---|---|---|
| Потолок 24ч объёма | $3 000 000 | **$500 000 000** | покрывает весь рынок альтов |
| Потолок цены | $10 | **выключен** | выбрасывал нормальные среднекапы |
| Сжатие | фикс. 1.4 %/15м | относительно своей нормы (≤0.7) | 1.4 % было истинно в 94 % случаев |
| Бонус за сжатие | +6 | **удалён**, вместо него +6 за расширение | сжатие антипредиктивно (1.4 % vs 15.9 %) |
| Бонус за крупный чек | +12 | **−6** при чеке ×6 | снижал попадания с 15.9 % до 11.1 % |
| Требование к тейкеру | ≥ 0.70 | **≥ 0.60** | 0.70 не давал ничего сверх 0.60, но резал выборку вдвое |
| «RVOL уже горячо» | штраф при RVOL ≥ 80 | штраф только если ×mult ≥ 1.5 | GENIUS печатал RVOL ×215 на первой минуте при ×1.1 |
| База сессии (×mult) | фикс. при первом появлении | **скользящая** (медиана за 6 ч со сдвигом 30 мин) | в суточном процессе всё превращалось в «chase» |
| Ускорение | наклон регрессии, одно значение | **окно 10 мин, R² цены и объёма, 3 растущие трети, дебаунс** | 0 из 40 000 блипов проходит |
| Аномалия объёма | RVOL ≥ фикс. порог | z-score с полом по средней минуте + RVOL + цена | голый z-score антипредиктивен |
| Накопление | не было | построено, бейдж 🐋, в score не входит | измеренное преимущество нулевое |
| Фон 1ч (RSI/BB/vol_z) | не было | +6 / +12 к score | из соседнего бэктеста, lift 13.9× |
| Telegram | EARLY + RVOL100 + SCORE×2 одновременно | **лестница из 3 тиров, 1 эпизод на пару, потолок 30 сообщений/час** | 9 сообщений → 3 на том же событии |
| Разгон на уже убежавшей паре | алертил | только в журнал (`accel_alert_min_score = 45`) | поздний вход при score < 45 |

---

## 9. Где что лежит

```
scanner-service/          новый сервис (запускать это)
  volscan/engine.py       вся детекция, без единого сетевого вызова
  volscan/feed.py         websocket + REST fallback + часовой контекст
  volscan/storage.py      SQLite WAL, пакетная запись
  volscan/alerts.py       Telegram: тип сигнала словами в первой строке
  volscan/dashboard.py    одностраничный адаптивный дашборд (ПК = телефон)
                          + вкладка «⚙ ФИЛЬТРЫ»: все пороги правятся на лету
  tools/replay.py         прогон исторических свечей через живой движок
  tests/                  20 офлайн-тестов, в т.ч. отбраковка блипов
  README.md               пошаговая инструкция по-русски

signals-research/         скрипты этого исследования (данные в .gitignore)
  binance_data.py         загрузка архива Binance Vision через S3
  build_universe.py       отбор корзины по дневному обороту
  scan.py / scan2.py      признаки + форвардные исходы по всей корзине
  study.py / study2.py    перебор порогов с walk-forward-срезами
  accel.py                математика детектора ускорения
  calib_accel.py          калибровка + синтетическая инъекция блипов
  genius_case.py          разбор пампа GENIUS 17.09.2026
  accum_case.py           разбор накопления KSM / G
  profile.py              профиль признаков вокруг события
  baseline.py             безусловные базовые частоты

index.html                СТАРЫЙ браузерный сканер, не тронут
STRATEGY.md               параллельное исследование (дневные/часовые свечи)
```

Воспроизведение: `cd signals-research && python3 download_1m.py && python3 scan2.py`
(нужны `numpy`; ~1.3 ГБ архивов кэшируется в `signals-research/cache/`,
всё это в `.gitignore`).

---

## 10. Итог одним абзацем

Из четырёх запрошенных детекторов **реально работает один** — непрерывное
одновременное ускорение цены и объёма (17.5 % попаданий на +10 %/4ч против
базы 0.83 %, проверено на незнакомых монетах и на незнакомом периоде, ноль
ложных срабатываний на 40 000 синтетических блипов, поминутно совпало с вашим
собственным алертом по GENIUS и дало 7.5 минуты форы до взрывной свечи).
«Сжатие» и «накопление» измеримого преимущества **не дают** — они построены,
показываются в интерфейсе как контекст, но в оценку не входят и в Telegram по
умолчанию не уходят; врать про них было бы дороже, чем признать. Аномалия
объёма работает только вместе с абсолютным полом и подтверждением ценой.
Всё это — история; **проверьте на живом рынке у себя**, сервис пишет данные
ровно для того, чтобы вы смогли пересчитать эти же цифры сами.
